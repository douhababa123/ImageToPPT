name: Test4

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    env:
      DOCKER_HOST: unix:///var/run/docker-nvme.sock
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Docker test
        run: docker ps